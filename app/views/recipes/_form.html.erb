<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %><br>

  <%=f.fields_for :recipes do |recip| %>
    <%= recip.label :name %>
    <%= recip.text_field :name %><br>

    <%= recip.label :name %>
    <%= recip.text_field :name %><br>
    <%end%>

    <% f.submit %>
<% end %>
